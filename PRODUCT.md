# Throw — 产品决策文档（PRODUCT.md）

> 本文是 Throw 产品目标、用户、核心流程、信息架构和视觉边界的唯一权威文档（canonical）。
> 代码、界面和 docs/ 下的工程文档必须与本文保持同步；产品边界变更先改这里。

## 一句话定位

无需安装、无需登录的**临时一对一传输房间**：像聊天一样互发文字和文件，用完即走，内容尽可能不经过云端存储。

## 目标用户

- 需要在两台设备或两个人之间快速传文件、截图、文本（剪贴板内容、密钥、验证码等）的用户
- 对隐私敏感、不希望文件留在第三方云端的用户
- 典型场景：手机 ↔ 电脑互传、同事/朋友之间临时共享

## 产品原则

1. **即开即用**：无注册、无安装、无昵称，打开网页即可创建或加入房间
2. **严格一对一**：每个房间最多两名成员（1 创建 + 1 加入），第三人无法加入
3. **临时性**：房间与内容不做长期保存。服务器只在必要时短暂中转文件，传输完成或房间关闭立即删除
4. **隐私优先**：文件优先 P2P 直传（不经过服务器）；仅当 P2P 不可用或明显更慢时走服务器中转
5. **有边界**：单文件 ≤ 100MB，单次批量 ≤ 10 个文件，文本 ≤ 8000 字符

## 非目标（明确不做）

- 多人房间 / 群聊
- 账号体系、好友关系、头像上传
- 消息与文件的云端历史、漫游、搜索
- 超过 100MB 的大文件
- 原生移动 App（仅响应式 Web）

## 核心流程

### 创建方

1. 打开首页 →（可选）填写昵称 → 点击「创建房间」
2. 获得 6 位房间码与邀请链接，进入等待页
3. 对方加入后进入房间，开始互发文本 / 文件
4. 任一方离开或房间超时 → 房间销毁，服务器中转文件立即删除

### 加入方

1. 在首页输入 6 位房间码，或直接打开邀请链接 `/r/:code`
2. （可选）填写昵称 → 加入 → 进入房间

### 传输（用户感知）

- 发送文件后，双方都能看到实时进度、速度与**通道标识**（P2P / 中转）
- 图片在消息流内显示缩略图，可放大预览；视频可内联播放；任意文件可下载保存
- 传输可取消；失败可重试（重试时自动换另一条通道）
- 会话为内存态：刷新页面等于离开（离开前有确认提示），已完成的本地接收内容随之丢失；房间在宽限期内可重连回去，但历史消息不恢复

## 信息架构

| 路由 | 页面 | 主要子状态 |
| --- | --- | --- |
| `/` | 首页：创建房间 / 加入房间 | — |
| `/r/:code` | 房间 | connecting / waiting / connected（P2P 或中转）/ peer-left / closed |

内容列 ≤ 720px 居中（移动全宽，桌面为聊天应用级列宽；首页入口卡片内部再收窄居中），输入区为 ChatGPT 风格胶囊卡片；界面语言为中文。

## 文案与视觉边界

- **工具型界面**：创建、连接、发送、进度、失败、重试等任务状态清晰可辨；不使用营销话术、装饰性区块和填充性卡片
- 图标统一使用 Lucide（`lucide-react`），禁止 emoji 充当图标
- 亮 / 暗主题跟随系统，可手动切换并持久化
- 聊天式布局，但心智模型是「会话即房间」：无历史记录、无未读、无通知系统
- 交互必须有明确主操作、重复提交保护、pending/success/failure 状态和可恢复错误

## 已确认的产品决策（2026-09-23；同日增补见下表）

| 决策点 | 结论 |
| --- | --- |
| 加入方式 | 6 位房间码 + 邀请链接（`/r/:code`）二合一 |
| 单文件上限 | 100MB |
| P2P 失败回退 | 服务器中转（上传 → 对方下载 → 即删），不自建 TURN |
| 会话持久性 | 全内存 + 临时文件，无数据库；房间关闭立即销毁并删除全部中转文件 |
| 开发阶段划分 | ① 设计文档 → ② 后端 `apps/api` → ③ 前端 `apps/web` |

### 增补决策（2026-09-23 晚，用户确认）

| 决策点 | 结论 |
| --- | --- |
| 无昵称设计 | 产品不提供昵称输入（接口不再接受）：全员游客，界面称「对方」 |
| 临时头像 | GitHub 风格 identicon：按「房间码:角色」确定性生成，双方本地独立计算且结果一致，服务端零存储 |
| 传输空闲关闭 | active 房间自最后一次**文件传输活动**起 30 分钟倒计时，到期自动销毁（前端顶栏展示倒计时，新传输重置）；文本消息不重置倒计时；等待页仍为 2 小时无人加入过期 |
